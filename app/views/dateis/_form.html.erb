<%= form_with(model: datei, local: true) do |form| %>
  <% if datei.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(datei.errors.count, "error") %> prohibited this datei from being saved:</h2>

      <ul>
        <% datei.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Name %>
    <%= form.text_field :Name %>
  </div>

  <div class="field">
    <%= form.label :Titel %>
    <%= form.text_field :Titel %>
  </div>

  <div class="field">
    <%= form.label :document %>
    <%= form.file_field :document, multiple: true %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
